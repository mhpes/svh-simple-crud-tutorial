/*
package es.mhp.ui.customcomponents;

import com.vaadin.data.fieldgroup.PropertyId;
import com.vaadin.ui.FormLayout;
import com.vaadin.ui.TextField;
import es.mhp.services.dto.AddressDTO;

*/
/**
 * Created by Edu on 29/02/2016.
 *//*

class CustomAddressForm extends FormLayout {

    public static final String ADDRESS_ID = "Address Id";
    public static final String MAIN_STREET = "Main Street";
    public static final String SECONDARY_STREET = "Secondary Street";
    public static final String CITY = "City";
    public static final String STATE = "State";
    public static final String LATITUDE = "Latitude";
    public static final String LONGITUDE = "Longitude";

    @PropertyId(ADDRESS_ID)
    TextField addressId = new TextField(ADDRESS_ID);

    @PropertyId(MAIN_STREET)
    TextField mainStreet = new TextField(MAIN_STREET);

    @PropertyId(SECONDARY_STREET)
    TextField secondaryStreet = new TextField(SECONDARY_STREET);

    @PropertyId(CITY)
    TextField city = new TextField(CITY);

    @PropertyId(STATE)
    TextField state = new TextField(STATE);

    @PropertyId(LATITUDE)
    TextField latitude = new TextField(LATITUDE);

    @PropertyId(LONGITUDE)
    TextField longitude = new TextField(LONGITUDE);

    public CustomAddressForm(AddressDTO addressDTO) {

        setSpacing(true);

        addComponent(addressId.setData(addressDTO.getAddressId()));
        addComponent(mainStreet);
        addComponent(secondaryStreet);
        addComponent(city);
        addComponent(state);
        addComponent(latitude);
        addComponent(longitude);
    }
}

*/
